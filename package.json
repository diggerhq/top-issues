{
  "name": "top-issues",
  "version": "1.0.0",
  "description": "Github top issues action =====",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npx @vercel/ncc build src/index.ts -o dist --source-map"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "octokit": "^3.2.0",
    "probot": "^13.2.0"
  }
}
